# Software-Stats SWE vs the World Notebooks (16–20) + HTML Report

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create and execute notebooks 16–20 (SWE relative advantage, purchasing power) and generate the final self-contained HTML report.

**Architecture:** Notebooks 16–19 compute SWE pay multiples relative to all peer roles. Notebook 20 shows what a SWE salary actually buys (cost-of-living purchasing power using embedded Big Mac index and rent proxies). generate_html.py extracts all Plotly charts into a single `docs/index.html`.

**Tech Stack:** Python 3.9+, pandas, plotly.express, plotly.graph_objects

**Prerequisites:** All previous plans complete — data-collection CSVs exist, analysis notebooks 04–15 executed.

**Repo location:** `/Users/mckornfield/repo/homelessness-stats/software-stats`

**Common role label map** (use in every notebook):
```python
ROLE_LABELS = {
    "software_engineer": "Software Engineer", "lawyer": "Lawyer",
    "physician": "Physician", "financial_analyst": "Financial Analyst",
    "registered_nurse": "Registered Nurse", "civil_engineer": "Civil Engineer",
    "construction_laborer": "Construction Laborer", "farm_worker": "Farm Worker",
    "manufacturing_worker": "Manufacturing Worker", "retail_worker": "Retail Worker",
}
```

---

### Task 1: Notebook 16 — SWE vs Peers USA

**Files:**
- Create: `notebooks/16_swe_vs_peers_usa.ipynb`

- [ ] **Step 1: Create notebook**

**Cell 1 (markdown):**
```
## USA: How Much Better Off Is a Software Engineer?

SWE mid-career pay as a multiple of each peer role. Values > 1 mean SWE earns more.
Blue-collar and agricultural roles show how far SWE pay diverges from manual work.
```

**Cell 2 (code — setup):**
```python
import sys
from pathlib import Path
ROOT = Path().resolve().parent
sys.path.insert(0, str(ROOT / 'src'))
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

ROLE_LABELS = {
    "software_engineer": "Software Engineer", "lawyer": "Lawyer",
    "physician": "Physician", "financial_analyst": "Financial Analyst",
    "registered_nurse": "Registered Nurse", "civil_engineer": "Civil Engineer",
    "construction_laborer": "Construction Laborer", "farm_worker": "Farm Worker",
    "manufacturing_worker": "Manufacturing Worker", "retail_worker": "Retail Worker",
}
SECTOR_COLORS = {
    "tech": "#2171b5", "legal": "#6a51a3", "healthcare": "#e6550d",
    "finance": "#31a354", "engineering": "#74c476",
    "blue_collar": "#969696", "agriculture": "#8c6d31",
    "manufacturing": "#c7e9c0", "services": "#fdae6b",
}

df = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_usa_data.csv')
mid = df[df["career_stage"] == "mid"].copy()
swe_mid = mid[mid["role"] == "software_engineer"]["median_salary_local"].values[0]
peers = mid[mid["role"] != "software_engineer"].copy()
peers["swe_multiple"] = (swe_mid / peers["median_salary_local"]).round(2)
peers["role_label"] = peers["role"].map(ROLE_LABELS)
peers = peers.sort_values("swe_multiple", ascending=True)
```

**Cell 3 (code — horizontal bar, SWE multiple):**
```python
fig = px.bar(
    peers, x="swe_multiple", y="role_label", orientation="h",
    color="sector", color_discrete_map=SECTOR_COLORS,
    title=f"USA: SWE Mid-Career Salary as Multiple of Each Role (BLS OES 2023)<br>"
          f"<sup>SWE median = ${swe_mid:,}/yr — values >1 mean SWE earns more</sup>",
    labels={"swe_multiple": "SWE / Role salary multiple", "role_label": "Role"},
    hover_data={"median_salary_local": ":$,.0f"},
)
fig.add_vline(x=1.0, line_dash="dot", line_color="red", annotation_text="Equal pay")
fig.show()
```

**Cell 4 (code — absolute gap bar):**
```python
peers["salary_gap_usd"] = swe_mid - peers["median_salary_local"]

fig2 = px.bar(
    peers.sort_values("salary_gap_usd", ascending=True),
    x="salary_gap_usd", y="role_label", orientation="h",
    color="sector", color_discrete_map=SECTOR_COLORS,
    title=f"USA: Annual Salary Gap — SWE Earns More By (USD, 2023)<br>"
          f"<sup>How much more a mid-career SWE makes vs each peer role per year</sup>",
    labels={"salary_gap_usd": "SWE annual salary advantage (USD)", "role_label": "Role"},
)
fig2.show()
```

- [ ] **Step 2: Execute**

```bash
cd /Users/mckornfield/repo/homelessness-stats/software-stats
.venv/bin/python -m papermill notebooks/16_swe_vs_peers_usa.ipynb notebooks/16_swe_vs_peers_usa.ipynb --cwd notebooks
```

- [ ] **Step 3: Commit**

```bash
git add notebooks/16_swe_vs_peers_usa.ipynb
git commit -m "feat: add SWE vs peers USA notebook"
```

---

### Task 2: Notebook 17 — SWE vs Peers India

**Files:**
- Create: `notebooks/17_swe_vs_peers_india.ipynb`

- [ ] **Step 1: Create notebook**

Same structure as notebook 16 but for India, using `merged_india_data.csv` and INR labels.

**Cell 1 (markdown):**
```
## India: How Much Better Off Is a Software Engineer?

SWE mid-career pay multiple over peer roles (NASSCOM/MOSPI 2023).
Salaries in INR. PPP comparison available in notebook 07.
```

**Cell 2 (code — setup):**
```python
import sys
from pathlib import Path
ROOT = Path().resolve().parent
sys.path.insert(0, str(ROOT / 'src'))
import pandas as pd
import plotly.express as px

ROLE_LABELS = {
    "software_engineer": "Software Engineer", "lawyer": "Lawyer",
    "physician": "Physician", "financial_analyst": "Financial Analyst",
    "registered_nurse": "Registered Nurse", "civil_engineer": "Civil Engineer",
    "construction_laborer": "Construction Laborer", "farm_worker": "Farm Worker",
    "manufacturing_worker": "Manufacturing Worker", "retail_worker": "Retail Worker",
}
SECTOR_COLORS = {
    "tech": "#2171b5", "legal": "#6a51a3", "healthcare": "#e6550d",
    "finance": "#31a354", "engineering": "#74c476",
    "blue_collar": "#969696", "agriculture": "#8c6d31",
    "manufacturing": "#c7e9c0", "services": "#fdae6b",
}

df = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_india_data.csv')
mid = df[df["career_stage"] == "mid"].copy()
swe_mid = mid[mid["role"] == "software_engineer"]["median_salary_local"].values[0]
peers = mid[mid["role"] != "software_engineer"].copy()
peers["swe_multiple"] = (swe_mid / peers["median_salary_local"]).round(2)
peers["role_label"] = peers["role"].map(ROLE_LABELS)
peers = peers.sort_values("swe_multiple", ascending=True)
```

**Cell 3 (code):**
```python
fig = px.bar(
    peers, x="swe_multiple", y="role_label", orientation="h",
    color="sector", color_discrete_map=SECTOR_COLORS,
    title=f"India: SWE Mid-Career Salary Multiple over Peer Roles (2023)<br>"
          f"<sup>SWE median = ₹{swe_mid:,}/yr</sup>",
    labels={"swe_multiple": "SWE / Role salary multiple", "role_label": "Role"},
    hover_data={"median_salary_local": ":,.0f"},
)
fig.add_vline(x=1.0, line_dash="dot", line_color="red", annotation_text="Equal pay")
fig.show()
```

**Cell 4 (code):**
```python
peers["salary_gap"] = swe_mid - peers["median_salary_local"]
fig2 = px.bar(
    peers.sort_values("salary_gap", ascending=True),
    x="salary_gap", y="role_label", orientation="h",
    color="sector", color_discrete_map=SECTOR_COLORS,
    title="India: Annual Salary Gap — SWE Earns More By (INR, 2023)",
    labels={"salary_gap": "SWE annual salary advantage (INR)", "role_label": "Role"},
)
fig2.show()
```

- [ ] **Step 2: Execute**

```bash
.venv/bin/python -m papermill notebooks/17_swe_vs_peers_india.ipynb notebooks/17_swe_vs_peers_india.ipynb --cwd notebooks
```

- [ ] **Step 3: Commit**

```bash
git add notebooks/17_swe_vs_peers_india.ipynb
git commit -m "feat: add SWE vs peers India notebook"
```

---

### Task 3: Notebook 18 — SWE vs Peers China

**Files:**
- Create: `notebooks/18_swe_vs_peers_china.ipynb`

- [ ] **Step 1: Create notebook**

Same structure as notebook 16/17 but for China, using `merged_china_data.csv` and CNY labels.

**Cell 1 (markdown):**
```
## China: How Much Better Off Is a Software Engineer?

SWE mid-career pay multiple over peer roles (NBS 2023). Salaries in CNY.
```

**Cell 2 (code — setup):**
```python
import sys
from pathlib import Path
ROOT = Path().resolve().parent
sys.path.insert(0, str(ROOT / 'src'))
import pandas as pd
import plotly.express as px

ROLE_LABELS = {
    "software_engineer": "Software Engineer", "lawyer": "Lawyer",
    "physician": "Physician", "financial_analyst": "Financial Analyst",
    "registered_nurse": "Registered Nurse", "civil_engineer": "Civil Engineer",
    "construction_laborer": "Construction Laborer", "farm_worker": "Farm Worker",
    "manufacturing_worker": "Manufacturing Worker", "retail_worker": "Retail Worker",
}
SECTOR_COLORS = {
    "tech": "#2171b5", "legal": "#6a51a3", "healthcare": "#e6550d",
    "finance": "#31a354", "engineering": "#74c476",
    "blue_collar": "#969696", "agriculture": "#8c6d31",
    "manufacturing": "#c7e9c0", "services": "#fdae6b",
}

df = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_china_data.csv')
mid = df[df["career_stage"] == "mid"].copy()
swe_mid = mid[mid["role"] == "software_engineer"]["median_salary_local"].values[0]
peers = mid[mid["role"] != "software_engineer"].copy()
peers["swe_multiple"] = (swe_mid / peers["median_salary_local"]).round(2)
peers["role_label"] = peers["role"].map(ROLE_LABELS)
peers = peers.sort_values("swe_multiple", ascending=True)
```

**Cell 3 (code):**
```python
fig = px.bar(
    peers, x="swe_multiple", y="role_label", orientation="h",
    color="sector", color_discrete_map=SECTOR_COLORS,
    title=f"China: SWE Mid-Career Salary Multiple over Peer Roles (NBS 2023)<br>"
          f"<sup>SWE median = ¥{swe_mid:,}/yr</sup>",
    labels={"swe_multiple": "SWE / Role salary multiple", "role_label": "Role"},
    hover_data={"median_salary_local": ":,.0f"},
)
fig.add_vline(x=1.0, line_dash="dot", line_color="red", annotation_text="Equal pay")
fig.show()
```

**Cell 4 (code):**
```python
peers["salary_gap"] = swe_mid - peers["median_salary_local"]
fig2 = px.bar(
    peers.sort_values("salary_gap", ascending=True),
    x="salary_gap", y="role_label", orientation="h",
    color="sector", color_discrete_map=SECTOR_COLORS,
    title="China: Annual Salary Gap — SWE Earns More By (CNY, 2023)",
    labels={"salary_gap": "SWE annual salary advantage (CNY)", "role_label": "Role"},
)
fig2.show()
```

- [ ] **Step 2: Execute**

```bash
.venv/bin/python -m papermill notebooks/18_swe_vs_peers_china.ipynb notebooks/18_swe_vs_peers_china.ipynb --cwd notebooks
```

- [ ] **Step 3: Commit**

```bash
git add notebooks/18_swe_vs_peers_china.ipynb
git commit -m "feat: add SWE vs peers China notebook"
```

---

### Task 4: Notebook 19 — SWE vs World Summary

**Files:**
- Create: `notebooks/19_swe_vs_world_summary.ipynb`

- [ ] **Step 1: Create notebook**

**Cell 1 (markdown):**
```
## SWE vs the World: Cross-Country Summary

Where is the SWE premium largest? PPP-adjusted comparison of SWE's pay advantage
over each peer role across USA, India, and China.
```

**Cell 2 (code — setup + combine):**
```python
import sys
from pathlib import Path
ROOT = Path().resolve().parent
sys.path.insert(0, str(ROOT / 'src'))
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

ROLE_LABELS = {
    "software_engineer": "Software Engineer", "lawyer": "Lawyer",
    "physician": "Physician", "financial_analyst": "Financial Analyst",
    "registered_nurse": "Registered Nurse", "civil_engineer": "Civil Engineer",
    "construction_laborer": "Construction Laborer", "farm_worker": "Farm Worker",
    "manufacturing_worker": "Manufacturing Worker", "retail_worker": "Retail Worker",
}
country_colors = {"USA": "#2171b5", "India": "#31a354", "China": "#e6550d"}

usa = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_usa_data.csv')
india = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_india_data.csv')
china = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_china_data.csv')
df = pd.concat([usa, india, china], ignore_index=True)
mid = df[df["career_stage"] == "mid"].copy()
mid["role_label"] = mid["role"].map(ROLE_LABELS)
```

**Cell 3 (code — SWE multiple over each role by country, using PPP USD):**
```python
swe_ppp = mid[mid["role"] == "software_engineer"][["country", "median_salary_ppp_usd"]].rename(
    columns={"median_salary_ppp_usd": "swe_ppp"}
)
peers = mid[mid["role"] != "software_engineer"].merge(swe_ppp, on="country")
peers["swe_multiple_ppp"] = (peers["swe_ppp"] / peers["median_salary_ppp_usd"]).round(2)
peers["role_label"] = peers["role"].map(ROLE_LABELS)

# Order roles by USA multiple
usa_order = (
    peers[peers["country"] == "USA"]
    .sort_values("swe_multiple_ppp", ascending=False)["role_label"].tolist()
)

fig = px.bar(
    peers, x="role_label", y="swe_multiple_ppp", color="country",
    barmode="group",
    category_orders={"role_label": usa_order, "country": ["USA", "India", "China"]},
    color_discrete_map=country_colors,
    title="SWE Pay Multiple vs Peer Roles by Country (PPP-Adjusted, Mid-Career, 2023)<br>"
          "<sup>Higher = SWE earns proportionally more than this role in that country</sup>",
    labels={"swe_multiple_ppp": "SWE / Role PPP Salary Multiple", "role_label": "Role", "country": "Country"},
)
fig.add_hline(y=1.0, line_dash="dot", line_color="red", annotation_text="Equal pay")
fig.update_layout(xaxis_tickangle=-35)
fig.show()
```

**Cell 4 (code — heatmap of SWE multiple by role × country):**
```python
heat = peers.pivot_table(index="role_label", columns="country", values="swe_multiple_ppp")

fig2 = px.imshow(
    heat, text_auto=True, aspect="auto",
    color_continuous_scale="RdYlGn",
    color_continuous_midpoint=1.0,
    title="SWE Salary Multiple by Role and Country (PPP-Adjusted, Mid-Career)",
    labels={"color": "SWE / Role multiple"},
)
fig2.show()
```

**Cell 5 (code — SWE multiple over median farm worker: the biggest gap):**
```python
farm_ppp = mid[mid["role"] == "farm_worker"][["country", "median_salary_ppp_usd"]].rename(
    columns={"median_salary_ppp_usd": "farm_ppp"}
)
swe_vs_farm = swe_ppp.merge(farm_ppp, on="country")
swe_vs_farm["multiple"] = (swe_vs_farm["swe_ppp"] / swe_vs_farm["farm_ppp"]).round(1)

fig3 = px.bar(
    swe_vs_farm, x="country", y="multiple", color="country",
    color_discrete_map=country_colors,
    title="SWE Mid-Career Salary vs Farm Worker — PPP Multiple (2023)<br>"
          "<sup>A software engineer earns X times what a farm worker earns</sup>",
    labels={"multiple": "SWE / Farm Worker PPP multiple", "country": "Country"},
    text="multiple",
)
fig3.update_traces(texttemplate="%{text}x", textposition="outside")
fig3.show()
```

- [ ] **Step 2: Execute**

```bash
.venv/bin/python -m papermill notebooks/19_swe_vs_world_summary.ipynb notebooks/19_swe_vs_world_summary.ipynb --cwd notebooks
```

- [ ] **Step 3: Commit**

```bash
git add notebooks/19_swe_vs_world_summary.ipynb
git commit -m "feat: add SWE vs world summary notebook"
```

---

### Task 5: Notebook 20 — Purchasing Power

**Files:**
- Create: `notebooks/20_purchasing_power.ipynb`

Embedded cost-of-living data (Big Mac index + monthly rent proxies, 2023):
- USA: Big Mac $5.58, avg 1-BR rent $1,500/mo
- India: Big Mac ₹190 (≈ $2.30 market rate), avg 1-BR rent ₹15,000/mo (metro)
- China: Big Mac ¥24 (≈ $3.40 market rate), avg 1-BR rent ¥3,500/mo (tier-1 city)

- [ ] **Step 1: Create notebook**

**Cell 1 (markdown):**
```
## What Does a SWE Salary Actually Buy?

Purchasing power comparison: how many Big Macs, monthly rents, and median local
wages does a software engineer's annual salary represent in each country?

Sources: The Economist Big Mac Index 2023; Numbeo cost-of-living 2023.
```

**Cell 2 (code — setup + embedded cost data):**
```python
import sys
from pathlib import Path
ROOT = Path().resolve().parent
sys.path.insert(0, str(ROOT / 'src'))
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

# Embedded cost-of-living proxies (local currency, 2023)
COST_DATA = {
    "USA":   {"big_mac_local": 5.58,   "monthly_rent_local": 1500},
    "India": {"big_mac_local": 190,    "monthly_rent_local": 15000},
    "China": {"big_mac_local": 24,     "monthly_rent_local": 3500},
}
country_colors = {"USA": "#2171b5", "India": "#31a354", "China": "#e6550d"}

usa = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_usa_data.csv')
india = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_india_data.csv')
china = pd.read_csv(ROOT / 'data' / 'processed' / 'merged_china_data.csv')
df = pd.concat([usa, india, china], ignore_index=True)
mid = df[df["career_stage"] == "mid"].copy()
swe = mid[mid["role"] == "software_engineer"].copy()

rows = []
for _, row in swe.iterrows():
    c = row["country"]
    salary = row["median_salary_local"]
    costs = COST_DATA[c]
    rows.append({
        "country": c,
        "annual_salary_local": salary,
        "big_macs_per_year": int(salary / costs["big_mac_local"]),
        "months_rent_per_year": round(salary / costs["monthly_rent_local"], 1),
    })
pp = pd.DataFrame(rows)
print(pp.to_string(index=False))
```

**Cell 3 (code — Big Mac purchasing power):**
```python
fig = px.bar(
    pp, x="country", y="big_macs_per_year", color="country",
    color_discrete_map=country_colors,
    title="SWE Annual Salary: How Many Big Macs? (2023, local currency)<br>"
          "<sup>Proxy for local purchasing power — more = better real purchasing power</sup>",
    labels={"big_macs_per_year": "Big Macs per year", "country": "Country"},
    text="big_macs_per_year",
)
fig.update_traces(texttemplate="%{text:,}", textposition="outside")
fig.show()
```

**Cell 4 (code — months of rent):**
```python
fig2 = px.bar(
    pp, x="country", y="months_rent_per_year", color="country",
    color_discrete_map=country_colors,
    title="SWE Annual Salary: How Many Months of (Metro) Rent? (2023, local currency)",
    labels={"months_rent_per_year": "Months of rent", "country": "Country"},
    text="months_rent_per_year",
)
fig2.update_traces(texttemplate="%{text:.1f} months", textposition="outside")
fig2.show()
```

**Cell 5 (code — SWE vs farm worker in Big Mac terms for each country):**
```python
farm = mid[mid["role"] == "farm_worker"][["country", "median_salary_local"]].rename(
    columns={"median_salary_local": "farm_salary"}
)
compare = pp[["country", "annual_salary_local"]].merge(farm, on="country")
compare["swe_bm"] = compare.apply(lambda r: int(r["annual_salary_local"] / COST_DATA[r["country"]]["big_mac_local"]), axis=1)
compare["farm_bm"] = compare.apply(lambda r: int(r["farm_salary"] / COST_DATA[r["country"]]["big_mac_local"]), axis=1)
long = pd.melt(compare[["country", "swe_bm", "farm_bm"]], id_vars="country",
               value_vars=["swe_bm", "farm_bm"],
               var_name="role", value_name="big_macs")
long["role"] = long["role"].map({"swe_bm": "Software Engineer", "farm_bm": "Farm Worker"})

fig3 = px.bar(
    long, x="country", y="big_macs", color="role",
    barmode="group",
    color_discrete_sequence=["#2171b5", "#8c6d31"],
    title="Annual Purchasing Power: SWE vs Farm Worker (Big Macs per year, 2023)",
    labels={"big_macs": "Big Macs per year (local currency)", "country": "Country", "role": "Role"},
)
fig3.show()
```

- [ ] **Step 2: Execute**

```bash
.venv/bin/python -m papermill notebooks/20_purchasing_power.ipynb notebooks/20_purchasing_power.ipynb --cwd notebooks
```

- [ ] **Step 3: Commit**

```bash
git add notebooks/20_purchasing_power.ipynb
git commit -m "feat: add purchasing power notebook"
```

---

### Task 6: Generate HTML report

**Files:**
- Run: `scripts/generate_html.py`
- Output: `docs/index.html`

- [ ] **Step 1: Run all remaining tests**

```bash
cd /Users/mckornfield/repo/homelessness-stats/software-stats
.venv/bin/python -m pytest tests/ -v
```

Expected: all tests pass.

- [ ] **Step 2: Confirm all 20 notebooks exist and have been executed**

```bash
python3 -c "
import json
from pathlib import Path
nb_dir = Path('notebooks')
for i in range(1, 21):
    nb = sorted(nb_dir.glob(f'{i:02d}_*.ipynb'))
    if not nb:
        print(f'MISSING: notebook {i:02d}')
        continue
    nb = nb[0]
    data = json.loads(nb.read_text())
    executed = any(
        cell.get('outputs') for cell in data['cells'] if cell['cell_type'] == 'code'
    )
    status = 'OK' if executed else 'NOT EXECUTED'
    print(f'{nb.name}: {status}')
"
```

Expected: all 20 show `OK`.

- [ ] **Step 3: Generate the report**

```bash
.venv/bin/python scripts/generate_html.py
```

Expected output:
```
Report written to /Users/mckornfield/repo/homelessness-stats/software-stats/docs/index.html
Size: NNN,NNN bytes
```

- [ ] **Step 4: Spot-check the HTML**

```bash
python3 -c "
html = open('docs/index.html').read()
print('Plotly divs:', html.count('Plotly.newPlot'))
print('Has USA section:', 'USA Pay' in html or 'BLS OES' in html)
print('Has India section:', 'India' in html)
print('Has China section:', 'China' in html)
print('Has SWE section:', 'Software Engineer' in html)
print(f'File size: {len(html):,} bytes')
"
```

Expected: 30+ Plotly divs, all sections present.

- [ ] **Step 5: Final commit**

```bash
git add docs/index.html
git commit -m "feat: generate final HTML report with all 20 notebooks"
```
